#include <stdio.h>
int main()
{
    int x, y;
    printf("Enter Value of x ");
    scanf("%d", &x);
    printf("\nEnter Value of y ");
    scanf("%d", &y);
  
    int  = x;
    x = y;
    y = x;
  
    printf("\nAfter Swapping: x = %d, y = %d", x, y);
    return 0;
}
